
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kidenai.app',
  appName: 'KidenAI',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
